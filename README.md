Инсталация:
pip install -r requirements.txt

Настройка:
копирай .env.example → .env
попълни ключовете

Създаване на колекции:
python schema.py

Зареждане на данни:
python load_data.py

Стартиране:
streamlit run app.py
